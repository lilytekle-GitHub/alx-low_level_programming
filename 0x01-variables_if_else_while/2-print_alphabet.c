#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = '97'; ch <= '122'; ch++)
	{
		putchar(low);
	}
	putchar(10);


	return (0);
}
